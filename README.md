# CYDI

Quick drawing challenges. Draw a shape, save it as a challenge, then try to redraw it from memory and get scored on how closely you match it.

Working codename — see `LEGAL_NOTES.md` before any public/commercial use.

## Commands

```bash
npm install
npm run dev      # start local dev server
npm run build    # type-check and build for production
```
